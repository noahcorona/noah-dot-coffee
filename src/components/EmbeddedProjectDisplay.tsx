import '../style/ProjectCard.css'

const EmbeddedProjectDisplay = (props: { project: any; }) => {
    const {project} = props;

    return (
        <div className="card-container">

        </div>
    )
}

export default EmbeddedProjectDisplay;
