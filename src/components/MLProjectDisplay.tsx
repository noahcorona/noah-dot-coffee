import '../style/ProjectCard.css'

function MLProjectDisplay(props: { project: any; }) {
    const {project} = props;

    return (
        <div className="card-container">

        </div>
    )
}

export default MLProjectDisplay;
