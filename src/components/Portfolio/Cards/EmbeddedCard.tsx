import '../../../style/ProjectCard.css'

const EmbeddedCard = (props: { project: any; }) => {
    const {project} = props;

    return (
        <div className="card-container">

        </div>
    )
}

export default EmbeddedCard;
