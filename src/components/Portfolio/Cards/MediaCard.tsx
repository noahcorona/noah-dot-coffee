import '../../../style/ProjectCard.css'

const MediaCard = (props: { project: any; }) => {
    const {project} = props;

    return (
        <div className="card-container">

        </div>
    )
}

export default MediaCard;
